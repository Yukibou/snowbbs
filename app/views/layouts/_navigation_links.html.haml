%ul.nav.navbar-nav
  %li= link_to '観察データ', observations_path
  - if user_signed_in? && current_user.admin?
    %li= link_to '雪崩情報', avalanche_infomations_path
  - if user_signed_in? && current_user.admin?
    %li= link_to 'Users', users_path
    %li= link_to 'Areas', areas_path
