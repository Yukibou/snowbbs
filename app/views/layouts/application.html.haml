!!!
%html
  %head
    %meta{:name => "viewport", :content => "width=device-width, initial-scale=1.0"}
    %title= content_for?(:title) ? yield(:title) : '雪の掲示板'
    %meta{:name => "description", :content => "#{content_for?(:description) ? yield(:description) : 'Snowbbs'}"}
    = stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => 'reload'
    = javascript_include_tag 'application', 'data-turbolinks-track' => 'reload'
    = csrf_meta_tags
  %body
    -#%header
    -#  = render 'layouts/navigation'
    .panel.panel-default
      .panel-body.for-iframe-0-padding
        %h4.for-iframe-title
          = link_to observations_path do
            .for-iframe-title
              雪の掲示板
              %small.font-white snow bulletin board
    %main.for-iframe{:role => "main"}
      = render 'layouts/messages'
      = yield
